分析完成，全部数据已交叉验证。简明报告如下：

## 分析报告

### 1. 文件与代码行统计
| 指标 | 数值 |
|---|---|
| 包目录数 | 500（pkg000–pkg499） |
| Python 文件总数 | **50,000**（每包 100 个 mod000–mod099） |
| 总行数 | **1,100,000 行**（每文件恒定 22 行：1 行注释 + 19 空行 + `def` + `return`） |
| 函数总数 | 50,000（每文件恰好 1 个 `fn_<pkg>_<mod>()`） |

### 2. “返回偶数”函数最多的包
**没有突出的赢家——500 个包完全并列。** 每个包的 100 个函数返回值恰好是 0–99 各一次（模运算验证），因此每个包都是 **50 个偶数返回 / 50 个奇数返回**：

- 偶数函数计数分布：`{50: 500}`（500 个包全部为 50）
- 全库合计：25,000 / 50,000 = 恰好 50%
- 并列第一（各 50/100）：pkg000, pkg001, pkg002, … 全部 500 个

> 注：第一版脚本曾把 `.git` 目录误计为一个“偶数为 0 的包”，已用 `pkg\d{3}` 过滤修正。

### 3. Git 工作区状态
```
On branch master
nothing to commit, working tree clean
```
✅ 工作区干净，与初始快照一致（仅 `a6ee1be5 init` 一个提交）。

### 结论
这是一个高度规整的合成数据集：结构完全均匀，偶数返回值在所有包中均匀分布，无法按该指标区分出“最多”的包。
