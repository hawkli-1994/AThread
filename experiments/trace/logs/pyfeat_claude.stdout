Every test-executing command is being blocked by the permission mode (`python3 --version` runs fine, but `python3 -m unittest …` and direct file execution all come back "requires approval"). Rather than keep probing for a way around the block, here's where things stand.

## What I did

**`calc/stats.py`** — matching `ops.py`'s compact style (no docstrings, simple loops):

- `mean(data)` — sums via loop, returns `total / len(data)`
- `median(data)` — sorts a copy, returns the middle element (odd) or average of the two middle elements (even)

**`tests/test_stats.py`** — 7 tests in the single-line style of `test_ops.py`, covering odd/even/single-element medians, unsorted input, and float means.

## Two things you should know

1. **I could not run the tests.** `python3 -m unittest discover -s tests -v` was denied by the permission settings, as were the equivalent invocations I tried (explicit module names, running the test file directly). The code is hand-verified but **not executed** — please approve that command (or run it yourself) to confirm.

2. **The suite has pre-existing failures that adding files can't fix.** `calc/ops.py:12` has a flagged bug — `div` uses floor division (`a // b`), so `test_div` (`div(7, 2)` → `3`, expected `3.5`) and `test_avg` (`avg([1, 2, 4])` → `2`, expected `~2.333`) fail regardless of my changes. Since you scoped me to *adding* files under `calc/` and `tests/`, I left `ops.py` untouched. Related: I deliberately built `mean` with true division instead of reusing `ops.avg`/`ops.div`, so it doesn't inherit that bug (`mean([1, 2])` is `1.5`, not `1`).

If you want the whole suite green, the fix is one line in `ops.py` (`a // b` → `a / b`) — say the word and I'll apply it once you've approved the test command so I can verify.
