• 验证时 import 产生了 `__pycache__`，我清理掉以恢复工作区原状：

• ## 结论

  **验证脚本运行结果：2000/2000 全部符合约定，0 个异常。**

  各步骤概况：

  1. **仓库结构**：`src/` 下确实有 `module00`–`module19` 共 20 个目录，每个目录含 `file000.py`–`file099.py` 共 100 个文件，总计 2000 个 Python 文件。文件格式为：若干注释行 + `import os/sys` + 一个 `foo_N()` 函数返回 `N`（注意：函数名实际是 `foo_0`、`foo_1` 这种不带零填充的写法，不是字面意义的 `foo_000`，返回值与文件编号一致）。

  2. **批量验证**：写了一个脚本（放在 `/tmp/verify_convention.py`，不在仓库内）用 `importlib` 逐个加载 2000 个文件，检查每个文件恰好定义一个 `foo_*` 函数，且调用返回值等于其文件编号。结果：**total: 2000, OK: 2000, BAD: 0**。

  3. **Git 状态**：仓库只有一个提交 `fe8a411 init`，2000 个文件全部被 git 追踪。验证过程中 import 生成的 `__pycache__` 目录已清理，最终 `git status` 完全干净，无任何修改或新增文件。

  4. **未修改任何文件**：脚本放在 `/tmp`，临时产物已删除，工作区与初始状态一致。

