• `python` 命令不可用，尝试 `python3`：

• 确认了 2 个失败：`test_div` 和 `test_avg`，根源都是 `div` 用了整除 `//`。修复：

• 全部完成。

  **问题**：`calc/ops.py:10` 的 `div()` 使用了整除 `//`，导致 `div(7, 2)` 返回 `3` 而非 `3.5`，`avg([1, 2, 4])` 也因此返回 `2` 而非 `7/3`。

  **修复**：将 `a // b` 改为 `a / b`（真除法），并删掉了过时的 BUG 注释。测试文件未做任何改动。

  **验证**：5 个测试全部通过（`Ran 5 tests ... OK`）。改动即上面的 `git diff`，仅 `calc/ops.py` 一个文件、一行实质修改。

